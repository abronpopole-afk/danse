name: Build Windows Installer

on:
  push:
    branches: [main, master]
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Fix package versions
        run: |
          (Get-Content package.json) -replace '"@auth/express": "\^0.10.4"', '"@auth/express": "^0.12.1"' | Set-Content package.json
          (Get-Content package.json) -replace '"@auth/core": "\^0.37.4"', '"@auth/core": "^0.40.0"' | Set-Content package.json
        shell: pwsh
          
      - name: Install dependencies
        run: npm install --ignore-scripts --legacy-peer-deps
        
      - name: Install Electron and build tools
        run: npm install --save-dev electron electron-builder --legacy-peer-deps
          
      - name: Build TypeScript workers
        run: npm run build:workers
        continue-on-error: true
        
      - name: Build application
        run: npm run build
        
      - name: Build Electron app
        run: npx electron-builder --win --x64
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: GTO-Poker-Bot-Windows
          path: dist-electron/*.exe
          retention-days: 30
          
      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: dist-electron/*.exe
          draft: true
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
